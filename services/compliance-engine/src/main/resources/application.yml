spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4
    vectorstore:
      pgvector:
        dimensions: 1536
        index-type: HNSW
  datasource:
    url: jdbc:postgresql://localhost:5432/regula
    username: postgres
    password: postgres
  redis:
    host: localhost
    port: 6379
server:
  port: 8081
